> Two implementing agents at most. **A** owns groups 1–3 (the shared contract, the Rust
> listener, the extension); **B** owns groups 4–6 (the webview) and cannot start before 1.1
> lands, since every event it subscribes to is named there. Group 7 is the owner's.
> Every `verify` names the test that pins the task; `mise run check` is the gate after each
> group.

## 1. Shared contract (`packages/shared` + `packages/app/src-tauri/src/model.rs`, one commit)

- [x] 1.1 Add `CaptureWithdrawn { id: string, reason?: string }` to `packages/shared/src/index.ts` and `model.rs` (serde camelCase, `reason` skipped when `None`); document on `CaptureMeta` that it is also the body of `POST /captures/pending` (design D2); name the two new events beside `CAPTURE_STORED_EVENT` — `capture:pending`, `capture:withdrawn` — in `src-tauri/src/commands.rs` and in `packages/app/src/lib/api/events.ts`; verify a serde test pins the camelCase JSON of `CaptureWithdrawn` with and without a reason, and `mise run typecheck` passes.

## 2. The listener relays and settles (`packages/app/src-tauri`)

- [x] 2.1 `http/mod.rs` + `lib.rs`: replace `HttpState.on_stored` with `on_event: Arc<dyn Fn(CaptureEvent) + Send + Sync>` over `enum CaptureEvent { Pending(CaptureMeta), Withdrawn(CaptureWithdrawn), Stored(ImageRecord) }`; `AppState::http_state` maps each variant to its event name; `test_support` records every event in order (design D6); verify the existing `a_stored_capture_is_announced_once_and_a_re_post_is_not` and `a_refused_capture_is_never_announced` pass against the recorder, restated over the enum.
- [x] 2.2 New `http/pending.rs`: `POST /captures/pending` takes `Json<CaptureMeta>`, answers 503 with no library open and nothing emitted, 400 on a body that is not a capture, otherwise emits `Pending` and answers 202 with no body; `DELETE /captures/pending/{id}` takes an optional `Json<{ reason }>`, emits `Withdrawn` and answers 204 for any id; both routed in `router` under the existing origin layer and body limit (design D2; spec `capture-ingest`, "A coming capture can be announced" and "An announced capture can be withdrawn"); verify tests that an announcement emits `Pending` with the fields sent and leaves `image_count` at 0, that no library gives 503 and no event, that a body without `id` gives 400 and no event, that a web-page origin gives 403 on both routes, that a withdrawal with a reason emits `Withdrawn` carrying it, and that a withdrawal for a never-announced id answers 204.
- [x] 2.3 `http/captures.rs`: every answer for a parsed `meta` settles — `Created` emits `Stored` only, `Existing` emits `Withdrawn` with no reason, an error from `store_now` emits `Withdrawn` with the error's message; a refusal inside `read_parts` emits nothing (design D6; spec `capture-ingest`, "Every answer to a capture settles its announcement"); verify tests that a new capture records `[Stored]` and no `Withdrawn`, that a re-post records `Withdrawn` for the id with no reason and no second `Stored`, that an undecodable image records `Withdrawn` whose reason is the 422's message, and that a missing `meta` part records nothing.

## 3. The extension announces first (`packages/extension`)

- [x] 3.1 `settings.ts` gains `pendingEndpoint(port)` and `pendingEndpoint(port, id)`; `delivery/client.ts` gains `announceCapture(endpoint, meta)` (JSON body, `Content-Type: application/json`, its own `ANNOUNCE_TIMEOUT_MS = 2_000`) and `withdrawCapture(endpoint, reason)` (`DELETE`, JSON `{ reason }`), both resolving to nothing whatever the app answers or fails to (design D5); verify client tests with a stubbed fetch that the announcement sends the meta as JSON to `/captures/pending`, that the withdrawal sends `DELETE` to `/captures/pending/<id>` with the reason, and that a network error, a 404 and a timeout each resolve without throwing.
- [x] 3.2 `background/deliver.ts`: `captureAndDeliver` mints `id` and `capturedAt` first, asks the page, awaits the announcement, then `captureImageBytes`; both routes failing withdraws with the reason before the existing notification; `retryCapture` is untouched (design D5; spec `capture-delivery`, "The app is told a capture is coming before its bytes are fetched"); verify deliver tests that the announcement's fetch happens before the `CAPTURE_IMAGE` message and before any request to the image host, that the announced id and `capturedAt` equal the posted entry's, that an announcement that rejects still delivers the capture, that both routes failing sends one withdrawal carrying the reason and creates no entry, that a retry makes no request to `/captures/pending`, and that the existing "no content script still delivers without a record" and "record reaches the meta on both paths" tests still pass.

## 4. Webview state (`packages/app/src/lib/api`)

- [x] 4.1 `events.ts`: `onCapturePending(handler: (meta: CaptureMeta) => void)` and `onCaptureWithdrawn(handler: (withdrawn: CaptureWithdrawn) => void)` beside `onCaptureStored`; verify `events.test.ts` cases that each payload reaches its subscriber unwrapped, matching the existing `capture:stored` test.
- [x] 4.2 New `pending.svelte.ts` exporting `pendingCaptures`: `subscribe()` listens to the three capture events and returns one unlisten; `entries` newest first; `capture:pending` adds (an id already present is replaced, not doubled), `capture:stored` and `capture:withdrawn` remove by id; each entry expires `PENDING_TTL_MS = 120_000` after it was added, with the argument from design D4 beside the constant and the clock injectable; verify store tests that an announcement appears at once, that stored and withdrawn each clear only their id, that a repeated announcement is one entry, that an entry expires at the bound and one settled before it does not, and that unsubscribing stops the timers.
- [x] 4.3 Move `components/library/imports.svelte.ts` to `lib/api/imports.svelte.ts` as the singleton `imports`: `enqueue(paths)` appends a run `{ id, paths, status, progress }` and starts the pump when idle; runs execute one at a time in order, each subscribing to `import:progress` before its command; `reports` is a list newest first with `dismiss(report)`; `onfinished(listener)` returns an unsubscribe and fires once per finished run; `pick(picker)` enqueues what the picker answers; `running`, `report` and the constructor callback go (design D9, D10; spec `local-file-import`, "Imports started during a run are queued"); verify store tests with a stubbed `importPaths` that a second `enqueue` during a run is `queued` and runs after the first resolves, that both runs leave their own report in order, that the listener fires per run, that `library.set` is called with the refreshed status per run, that empty paths enqueue nothing, that a cancelled picker enqueues nothing and sets no error, and that a failing run records its error and the next queued run still starts.

## 5. Webview UI (`packages/app/src/lib/components/library`, `packages/app/src/routes`)

- [x] 5.1 `pnpm dlx shadcn-svelte add progress`; new `PendingBand.svelte` rendering `PendingCaptureTile.svelte` per `pendingCaptures.entries` and `ImportRunTile.svelte` per `imports.runs`, laid out with `gridWindow`'s columns, `EDGE` and `GAP` at the route's `tile`, rendered only while it has tiles; the capture tile is a `skeleton` square naming the adapter site and the page title, the run tile shows the counting line, the `progress` bar with done of total and the imported count, or "Waiting" with the item count (design D7, D8; spec `pending-work`); verify by the owner's pass in 7.1 and by `mise run check` (lint, typecheck, build) — the repo has no component tests, and the state each tile reads is pinned in 4.2 and 4.3.
- [x] 5.2 Wire the routes: `+layout.svelte` calls `pendingCaptures.subscribe()` for the life of the window beside its `onCaptureStored` subscription; `+page.svelte` drops `new Imports(...)`, imports the singleton, registers `imports.onfinished(() => results.refresh())` in an `$effect`, enqueues on drop, renders `PendingBand` above the results area (grid, empty state and "nothing found" alike), and renders `ImportReportCard` per report; `ImportMenu.svelte` loses `disabled` and the progress paragraph and calls `imports.pick`; `ImportReportCard.svelte` takes the report it shows and its own `ondismiss` (design D9–D11; spec `local-file-import`, "Menu during a run"); verify `mise run check` green and `pnpm --filter app test` green.

## 6. Gate

- [x] 6.1 `mise run check` green from a clean tree, `mise run build` emits `packages/extension/dist` for the owner to reload.

## 7. Manual pass (owner)

- [x] 7.1 With the rebuilt extension reloaded and the app running on a library: capture from an X post — a placeholder naming the post appears in the band on the click and is replaced by the image in the grid; quit the app, capture — the extension entry is `failed` as before and the app, reopened, shows no placeholder; drop a folder of a few hundred files, then drop another during the run — the second tile says it waits, the Import button stays enabled, the menu opens, both runs finish with their own report; open settings during a run and come back — the band is still there with the count moved on; capture an undecodable image (an SVG) — the placeholder goes, the popup entry carries the 422's reason.

## 8. What the manual pass found (`packages/app/src-tauri`)

- [x] 8.1 `commands.rs`: one helper that runs `with_library` work on `tauri::async_runtime::spawn_blocking` and answers its `Result` (the shape `captures::store` uses, with its argument moved to the helper); `search`, `thumbnail_path`, `library_status`, `image_counts`, `drop_image_record` and `reveal_library` become `async fn` on it, and `import_paths` uses it instead of its own `spawn_blocking`; `status()` reads the library under the helper too (design D13; spec `pending-work`, "The window stays responsive while work is in flight"); verify the existing command tests still pass through the async signatures (Tauri's `test` feature runs them on a runtime), plus one new Rust test that a `search` resolves while another thread holds the library mutex for 300 ms and releases it — asserting the search's answer arrived after the release, not that the main thread was free, which no unit test can see.
- [x] 8.2 `import.rs` + `commands.rs`: `import::import_paths` takes `&SharedLibrary` and locks per candidate file inside its loop through `with_library`, never across the loop; the walk and the progress callback stay outside the lock (design D13); verify the existing `import_paths_reports_every_file_and_ticks_progress` still passes, and a new test that a `search` on another thread completes while an import of three files is running rather than after it.
- [x] 8.3 `ingest.rs` + `thumbs.rs` + `commands.rs`: `store_image` returns before the thumbnail, and its callers (`captures::store_now`, `import::import_file`) generate it after the lock is released; `thumbnail_path` reads the record under the lock and generates the thumbnail outside it (design D13); verify the ingest tests that pin the thumbnail's presence after a store still pass, and `cargo clippy --all-targets -- -D warnings` is clean.
- [x] 8.4 `mise run check` green; the owner re-runs the three-captures-at-once and the drop-during-scroll steps.
