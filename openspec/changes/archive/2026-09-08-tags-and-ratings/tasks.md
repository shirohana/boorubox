> Depends on `app-shell` being implemented: the Inspector, the sidebar `filters` region, the
> toolbar `view` row and `isTypingTarget` all come from it, and this change writes only into
> the slots its design.md names (design D17).
>
> Two implementing agents, split by file ownership: **A** owns group 1 (`packages/shared`,
> `packages/app/src-tauri`, `packages/app/src/lib/api/`, and the deletions under
> `src/lib/domain/`), **B** owns groups 2–5 (`src/lib/components/`, `src/lib/domain/
> tag-input.ts` and `tag-utils.ts`, `src/routes/`). Group 1 lands first — every component in
> 2–5 calls its commands — except that 2.2 and 2.3 are pure modules with no dependency on it
> and can start immediately.

## 1. Contract, writes and queries (agent A)

- [x] 1.1 `packages/shared` + `packages/app/src-tauri`: add `SortField` (`captured` | `updated` | `size` | `dimensions`), `SortDirection`, `Sort`, `GroupBy` (`none` | `x-account` | `duplicates`), `GroupSlice { key, count }`, `TagCount { name, count }`, `RatingCounts { g, s, q, e, unrated }` and `TagCounts { tags, ratings }` to `src/index.ts`, give `SearchRequest` a `sort` and a `group` and `SearchResult` a `groups`, and mirror all of it in `model.rs` (Phase 1 D11 — one commit, both files); verify a serde test asserts the camelCase keys and the `x-account` spelling, and `mise run typecheck` passes.
- [x] 1.2 `packages/app/src-tauri`: new `tags.rs` with the one tag-write path — replace an image's whole set in a transaction, extract `rating:[gsqe]` tokens into the rating (design D3), stamp `updated_at` (design D9), and collect the tags the write unlinked (design D5); verify unit tests for adding, replacing, clearing, a repeated tag collapsing to one, `rating:s` setting the rating and not being stored, `rating:unknown` being stored as a tag, a tag shared with another image surviving the unlink, and a tag whose last use was removed leaving `tags`.
- [x] 1.3 `packages/app/src-tauri`: `tags::set_rating` — write `g`/`s`/`q`/`e` or NULL, stamp `updated_at`, reject anything else (design D11); verify unit tests that each of the five values round-trips through a search on `rating:` and `is:unrated`, and that an unknown value returns an error and leaves the row unchanged.
- [x] 1.4 `packages/app/src-tauri`: `maintenance::drop_image_record` collects orphan tags in the same transaction, deleting the FIXME the change owns; verify a test that dropping the only image carrying a tag removes the `tags` row while a tag shared with another image stays.
- [x] 1.5 `packages/app/src-tauri`: `tags::suggestions(prefix, limit)` — prefix match on tag names, most used first then by name, capped at `limit` (design D12); verify unit tests for a prefix matching several tags in usage order, a prefix matching none returning an empty list, an empty prefix returning the most used, and case-insensitive matching.
- [x] 1.6 `packages/app/src-tauri`: `query::tag_counts(req)` — the tag half over the fully filtered set and the rating half with the rating clause dropped, both ignoring `limit`/`offset`, in one call (design D8); verify tests ported from `filters.test.ts`'s `computeRatingCounts` block — counts across the filtered set, and the rating pills unaffected by the search's own `rating:` metatag — plus one asserting tag counts describe the whole result and not a page.
- [x] 1.7 `packages/app/src-tauri`: `query::search` honours `sort`, mapping the four fields to fixed column expressions with `id DESC` breaking every tie (design D6); verify tests ported from `filters.test.ts`'s `sortImages` block — capture time both directions, last change, file size, pixel area — plus a paging test over rows with identical sort values asserting each row appears exactly once.
- [x] 1.8 `packages/app/src-tauri`: grouping in `query.rs` — the group restriction in the `WHERE`, the group key ahead of the sort in the `ORDER BY`, and `groups` from one `GROUP BY`, with the matched set emitted as a CTE for the duplicates case (design D7); verify tests ported from `grouping.test.ts` — accounts ordered by size then name with non-account pages dropped, duplicate triples of two or more with singletons dropped, and `total` matching the sum of the slice counts.
- [x] 1.9 `packages/app/src-tauri` + `packages/app/src/lib/api`: commands `update_tags(id, tags)`, `set_rating(id, rating)`, `tag_suggestions(prefix, limit)`, `tag_counts(req)` returning the reloaded `ImageRecord` where an image was changed (design D10), plus one wrapper each in `commands.ts` and the `index.ts` exports; verify command tests on the mock runtime for each, including `NoLibrary` with nothing open and `NotFound` for an unknown id, and a `commands.test.ts` case per wrapper asserting the command name and argument keys.
- [x] 1.10 `packages/app/src/lib/domain`: delete `filters.ts`, `filters.test.ts`, `grouping.ts` and `grouping.test.ts`, moving the `getXAccountFromUrl` URL table into `query.rs`'s `x_account` tests and deleting the "two implementations" FIXMEs in all three files (design D15; `parseSearchQuery`'s cases are deliberately not ported); verify `mise run check` is green and nothing under `packages/app/src` still imports either module.

## 2. Tag and rating controls (agent B)

- [x] 2.1 `packages/app`: add the shadcn-svelte copy-ins these controls need — `pnpm dlx shadcn-svelte@latest add command popover context-menu toggle-group`; verify the files land under `src/lib/components/ui/`, `mise run lint` still passes with them excluded (CLAUDE.md), and the app builds.
- [x] 2.2 `packages/app/src/lib/domain/tag-input.ts`: the autocomplete rules as pure functions over `(value, caret)` — current token, exclusion prefix, metatag and `or` suppression, replacing a token with a suggestion, completing a token, and which of accept / complete / submit a confirmation means (design D12, D13); verify unit tests for each rule, including the `-cathe` → `-cathedral` case, a caret in the middle of a query, and the three-confirmation sequence.
- [x] 2.3 `packages/app/src/lib/domain/tag-utils.ts`: add `addTagToQuery`, `excludeTagFromQuery`, `toggleTagInQuery` and `toggleRatingInQuery` beside the parser and `removeTagFromQuery` (design D14); verify tests that each result re-parses to the intended `ParsedTagSearch`, that the rest of the query survives, that toggling a rating rewrites the whole `rating:` metatag, and that the unrated toggle adds and removes `is:unrated`.
- [x] 2.4 `packages/app/src/lib/components/tags/TagInput.svelte`: the input plus the suggestion popover, driven entirely by `tag-input.ts` and `tag_suggestions`, usable both as the toolbar's tag search and as the Inspector's editor; verify by hand in the running app that typing a prefix lists suggestions in usage order, that the arrows and `Tab` select, that `Escape` closes without changing the text, and that the three-confirmation sequence behaves as spec'd (spec `tag-editing`).
- [x] 2.5 `packages/app/src/lib/components/tags/RatingControl.svelte`: the five choices with the current one shown, calling `set_rating`; verify by hand that each value and the clear round-trip against a search for `rating:` and `is:unrated`.

## 3. The Inspector and the tile (agent B)

- [x] 3.1 `packages/app/src/lib/components/library/Inspector.svelte`: the read-only tag list becomes `TagInput` plus the saved tags, each clickable into the search and each carrying a remove entry in its context menu, sorted for display with `sortTags` (design D4, Slot: Inspector · tags); verify by hand that a save from the grid-side Inspector and from the lightbox's inspect mode both change the image and that the editor's text comes back sorted.
- [x] 3.2 `packages/app/src/lib/components/library/Inspector.svelte`: the read-only rating becomes `RatingControl` (Slot: Inspector · rating); verify by hand that the tile's badge and the Inspector agree immediately after a change.
- [x] 3.3 `packages/app/src/lib/components/library/ImageCard.svelte`: the rating badge on rated tiles only, and a context menu setting the rating (Slot: Grid · tile, design D17); verify by hand with one rated and one unrated image that only the rated one carries a badge, and that the menu rates an image that is not the one in the Inspector.
- [x] 3.4 `packages/app/src/lib/api/search.svelte.ts`: replace an edited record in place from the command's return value and refresh the sidebar counts, without re-running the search (design D10); verify a unit test that an edit updates the record at its index, leaves `total`, `generation` and the loaded pages alone, and that removing the searched-for tag leaves the image on screen until the next search.

## 4. The sidebar (agent B)

- [x] 4.1 `packages/app/src/lib/api/search.svelte.ts`: carry the current result's `TagCounts`, fetched with each search from the same inputs and cleared when the query changes (design D8); verify a unit test that the counts and the result come from the same request and that a stale answer from a superseded query is discarded.
- [x] 4.2 `packages/app/src/lib/components/tags/TagSidebar.svelte`: the tag list with counts, ordered active-first then by count then by name, with the query's own tags added at zero, and `+` / `−` / click acting through the `tag-utils` mutations (spec `tag-sidebar`); verify by hand that counts match a known library, that an excluded tag with no matches stays listed at zero, and that clicking an active tag removes it from the query.
- [x] 4.3 `packages/app/src/lib/components/tags/RatingPills.svelte`: the five pills with the counts the rating half of `tag_counts` returns and the toggle from `tag-utils` (spec `tag-sidebar`); verify by hand that with `rating:s` in the query the other pills still show non-zero counts, and that toggling two ratings asks for both.
- [x] 4.4 `packages/app/src/lib/components/frame`: mount both in the sidebar's `filters` region, absent while no library is open (Slot: Sidebar · filters); verify by hand that `/start` shows no filters region and the library screen shows both panels with the sidebar's scroll behaviour.

## 5. Sort and grouping in the grid (agent B)

- [x] 5.1 `packages/app/src/lib/api/search.svelte.ts`: carry `sort` and `group` as session state, put them in every `SearchRequest`, and expose the returned `groups`; verify unit tests that changing either re-runs the query from the top and that the defaults are newest capture first, ungrouped.
- [x] 5.2 `packages/app/src/lib/components/library`: the sort select and the group select in the toolbar's view row beside the tile-size slider (Slot: Toolbar · view); verify by hand that each of the four sorts and both directions reorder the grid, and that the choice survives a new search but not a restart (spec `sort-and-group`).
- [x] 5.3 `packages/app/src/lib/components/library/grid-window.ts` + `LibraryGrid.svelte`: derive the row layout from the group slices — a heading row per group with its count, then that group's tiles — so a heading is right before its page is loaded (design D7); verify `grid-window.test.ts` gains cases for the grouped layout (row count and the index of the first tile of the fortieth group) and still asserts that rendered rows depend on the viewport, not on the result size.

## 6. Verification

- [x] 6.1 `mise run check` passes (lint, typecheck, tests, clippy, builds).
- [x] 6.2 Manual pass in the running app on a library with a few hundred images: edit the tags of one image from the Inspector and again from the lightbox's inspect mode; see the new tag appear in the sidebar with its count; click it to search and click it again to clear; exclude one from the sidebar; remove a tag from its context menu; set a rating from the Inspector and another from a tile's context menu and see the badges; check the rating pills still count the other ratings while `rating:` is in the query; sort by each of the four fields both ways; group by X account and by duplicates and scroll into a group far down the result.
- [x] 6.3 Regression pass: the Phase 1 end-to-end list still holds — a `curl` capture (201), a retry of the same id (200, one image), a folder import, tag and free-text search, the empty state naming the query, and a file deleted under `images/` showing the missing card with its drop action — and dropping that record now leaves no orphan tag behind (check `SELECT COUNT(*) FROM tags WHERE id NOT IN (SELECT tag_id FROM image_tags)` is 0).

## 7. Owner's pass (2026-09-08)

What the manual pass in 6.2 turned up that this change owns. Items the owner deferred as not
this change's (card drag hitting the dropzone, lightbox focus and key handling, Up/Down in the
viewer, sidebar edge resize, local imports stamped with the file's mtime) are planned as
`browse-polish`, not here.

- [x] 7.1 `packages/app/src/lib/components/library/ImageCard.svelte`: the tile has no aspect ratio — the shadcn `context-menu-trigger` copy-in sets `class` (`cn-context-menu-trigger select-none`), bits-ui merges it into the child snippet's `props`, and the spread after `class="aspect-square"` replaces it (computed `aspect-ratio: auto`, verified in the running app), so each tile takes its thumbnail's height and the next row paints over it. Fix: pass `class="aspect-square"` to `ContextMenu.Trigger` so the copy-in's `cn` merges it, and leave a one-line warning at the child that a class on the element itself is replaced by the merged props; verify by hand that every tile is square again at two tile sizes.
- [x] 7.2 `packages/app/src/lib/components/frame/Sidebar.svelte`: Library and Settings move to the bottom of the sidebar, directly above the library footer, so the filters region (what the owner reaches for) is at the top; collapsed to the icon rail they stay visible as icons; verify by hand on `/` and `/settings` that the active item still highlights and that `/start` (no filters) does not look broken.
- [x] 7.3 `packages/app/src/lib/components/tags/ratings.ts` + `RatingPills.svelte` + `RatingControl.svelte`: an inactive rating is drawn in a dimmed version of its own colour (text and border tinted, no fill) instead of muted grey, so the four are distinguishable in both states; the dim variants live beside `RATING_COLOUR` in `ratings.ts` and both controls read them from there; the active state and the tile badge are unchanged; verify by hand in light and dark theme.
- [x] 7.4 `packages/app/src/lib/components/tags/TagSidebar.svelte`: an included tag gets a visible active style (accent background and medium weight, sidebar accent tokens), as visible as the excluded state's red strike-through; verify by hand that after clicking a tag its row is obviously active and that clicking it again clears it.
- [x] 7.5 `packages/app/src/lib/domain/tag-input.ts` + `tag-input.test.ts` + design D13 + spec `tag-editing`: accepting a suggestion (Enter, Tab or click) finishes the token — the tag is followed by one space, stepping over one already there — and leaves the caret after it, so the next keystroke starts a new token and the next list can open. D13's "no space on accept" argument is overturned in place: it kept the confirmation count uniform, but in use it leaves the caret glued to the word (`apple ball|`), no list opens until a space is typed by hand, and a second Enter only inserts what the accept should have. The spec's "Accept, then finish, then submit" scenario becomes accept-then-submit; a typed token nobody accepted still completes on the first confirmation. Verify unit tests for accept at the end, accept in the middle of a query before a space, accept of an `-` token, and the two-confirmation sequence.
- [x] 7.6 `packages/app/src/lib/components/tags/TagInput.svelte` + `Inspector.svelte`: a confirmation that submits the Inspector's editor saves and, once the save has succeeded, takes the focus off the field (so the grid's keys work again); a failed save keeps the focus and shows the error; the toolbar's search keeps its focus after Enter as before. Verify by hand.
- [x] 7.7 `packages/app/src/routes/+page.svelte` + `RatingPills.svelte` + `TagSidebar.svelte`: the filters region stays mounted while a search runs — both headings and the five rating pills stay in place with their counts blank (not the last query's numbers, design D8) and the tag list shows nothing until the counts arrive — instead of unmounting both panels whenever `results.counts` is null, which flickers the whole sidebar on every click. `search.svelte.ts` keeps clearing `counts` (its 4.1 test stands). Verify by hand that clicking a tag or a pill no longer blanks the region.
- [x] 7.8 `mise run check` green.

Second look by the owner, same day:

- [x] 7.9 `TagInput.svelte`: the highlighted suggestion is invisible on the first row — every row painted out the command's own `data-selected` highlight with a variant class, which outranks the plain `bg-muted` the highlighted row gets, and the command always marks the first row selected. Paint it out only on rows that are not highlighted.
- [x] 7.10 `ratings.ts`: the active rating in the Inspector's toggle group showed as white text on grey — the toggle copy-in's on-state and hover `bg-muted` are variant classes and outrank the fill. The fill in `RATING_COLOUR` carries `!`, with the reason beside it.
- [x] 7.11 `TagSidebar.svelte`: a small gap between rows, so two adjacent active rows do not read as one block.
- [x] 7.12 `Sidebar.svelte`: a small gap between the Library and Settings items.
- [x] 7.13 `RatingPills.svelte`: a four-column grid with `none` spanning the row, counts right-aligned, so a count growing a digit no longer reflows the pills onto two lines and jumps the sidebar.
- [x] 7.14 `TagSidebar.svelte`: an included tag is green (fill and text), an excluded one red with a faint fill — the `+` / `−` reading, which the owner finds easier to scan than the neutral accent.
