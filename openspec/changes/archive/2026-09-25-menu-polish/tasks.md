> Two Sonnet units in parallel on disjoint files (retried on Opus if the gate fails), then one
> Opus review of both diffs. Design D1–D7 decide every shape; do not re-decide them. Unit A owns
> `Inspector.svelte`, `TagVocabularyMenuItems.svelte`, `TagSidebar.svelte`, the new
> `domain/danbooru.ts` (+ test) and the `pinned-tag-groups` archive design. Unit B owns
> `ImageCard.svelte`, `app.css`, `CollectionsSection.svelte`, `StampBar.svelte` and the
> `trash` archive design. Neither touches the other's files or `components/ui`. Gate for a
> webview unit: `pnpm lint && pnpm typecheck && pnpm test` from the repo root (the Rust half of
> `mise run check` is untouched by both); `.svelte-kit` is regenerated by typecheck and test, so
> run the gate once at the end and, if it fails on a generated-file error while the other unit
> is also gating, rerun it once. Units do not commit and do not tick hand checks.

## 1. Unit A — group hint, Danbooru look-up, tag-menu order (`packages/app`)

- [x] 1.1 `src/lib/domain/danbooru.ts` + `danbooru.test.ts`: `danbooruLookup(name, category)` per
      D2 (host constant, artist search vs wiki page, `encodeURIComponent`). Tests: an artist
      name yields the artists search URL with the two bracketed params encoded; a general tag
      yields `wiki_pages/<name>`; a name containing `/` and a Japanese name are encoded.
- [x] 1.2 `TagVocabularyMenuItems.svelte`: the Danbooru item first with `ExternalLinkIcon`,
      label and URL from `danbooruLookup(name, category)`, opened through `openExternal`
      (`$lib/api`), result ignored with the D2 comment and FIXME; then a `Separator`; then Pin /
      Unpin with `PinIcon` / `PinOffIcon` (D4); the group items unchanged and without icons; the
      Category checkbox items each render `CATEGORY_ICON[option]` (import from
      `./categories`) before the label. Header comment updated (it lists what the component
      holds).
- [x] 1.3 `Inspector.svelte` `pinnedTagRows` snippet per D1: `#n` span and `aria-label` only
      when `vocabulary.pinnedGroups.length > 1`; the D5 comment on the snippet rewritten to say
      what is drawn and why (a hint, not a label; hidden for one group), citing the owner's
      2026-09-25 reason in one clause.
- [x] 1.4 `Inspector.svelte` tag-list menu per D7: `tagSearchItems`, separator,
      `TagVocabularyMenuItems`, separator, "Remove from this image" last (still `destructive`).
- [x] 1.5 `openspec/changes/archive/2026-09-24-pinned-tag-groups/design.md` D5: append a
      dated paragraph recording the amendment (why "no number" was right at three groups and
      stopped being right at eight; the number is the menu's own, not a name). Do not rewrite
      the original text.
- [x] 1.6 Gate green. Handoff: the exact class string used for the hint and any deviation.
- [ ] 1.7 Hand check (owner): with two or more groups, each row shows a dim `#n` at its right
      edge, on both strips; with one group, none. "Open Danbooru wiki" on `solo` opens the wiki
      in the browser; "Search artist on Danbooru" on an artist tag opens the artist search.
  Seen (lead's smoke, 2026-09-25, scratch copy of test-1, driven through CGEvents): two pins
  in one group drew no number; "New group below" gave two rows with a dim `#1` / `#2` at each
  row's right edge under the hairline; "Move to #1" removed the hint. The inspector menu on a
  general tag listed "Open Danbooru wiki" with the external-link icon after the two search
  items, then Pin with its icon, the Category items with their five icons, Remove last; the
  sidebar menu listed it too; an artist tag offered "Search artist on Danbooru". The Danbooru
  items were not chosen (they open the browser). Screenshots: `boorubox-vault/smoke-2026-09-25/C4-two-groups.png`,
  `B2-inspector-general-menu.png`, `B4-sidebar-general-menu.png`.

  Hand check: not run against the live app (no dev server, per the gate boundary). Look at,
  with two-plus pinned groups: the `#n` sits at the row's top right on both the panel strip and
  the lightbox strip, dim and readable against the chips wrapping under it, and at one group no
  space is reserved for it (no visual gap where it would sit). Look at: "Open Danbooru wiki" on
  a non-artist tag and "Search artist on Danbooru" on an artist tag each open the system browser
  at the URL `danbooru.ts` builds — `pnpm --filter @boorubox/app tauri dev` was not run this
  session.

## 2. Unit B — menu density, icons, rating pills, red trash (`packages/app`)

- [x] 2.1 `src/app.css`: the D3 block, one comment above it saying why the override lives here
      (the copy-in argument the sidebar block already makes) and that it covers both menu
      kinds on purpose. Selectors by `data-slot` exactly as D3 lists them.
- [x] 2.2 `ImageCard.svelte` per D5: the rating pills row inside the existing group, `image`'s
      current rating filled; `none` neutral; `aria-label`/`title` from `ratingLabel`; the
      heading stays. The comment above the group extended by one sentence saying the items are
      pills but still menu items and why (Enter picks and closes; arrow keys step them).
- [x] 2.3 `ImageCard.svelte` per D4/D6: "Move to trash" `variant="destructive"` with
      `Trash2Icon`; "Delete forever…" gains `Trash2Icon`; "Restore" gains `Undo2Icon`; no
      confirmation added anywhere. The comment that says "The reversible action is plain; the
      irreversible one is marked" is rewritten to the new rule: both red, only the irreversible
      one has the ellipsis and confirms.
- [x] 2.4 `CollectionsSection.svelte`: "Rename…" gains `PencilIcon`, "Delete…" gains
      `Trash2Icon`; `StampBar.svelte`: "Edit…" `PencilIcon`, "Delete…" `Trash2Icon`. Nothing
      else in those menus changes.
- [x] 2.5 `openspec/changes/archive/2026-09-10-trash/design.md` D13: append a dated paragraph
      recording the amendment (red on the reversible act for identity; the ellipsis and the
      confirmation remain the marks of the irreversible one).
- [x] 2.6 Gate green. Handoff: whether the pills needed the `!` overrides against the item's
      `focus:bg-accent`, and the exact pill classes.
- [ ] 2.7 Hand check (owner): right-click a tile; the Rating heading is dim and smaller than
      the items, the five pills show the current rating filled, type `q` then Enter, or click
      `q`, rates and closes; arrow keys still move through the menu; "Move to trash" is red and
      moves at once. The Library menu (top-left dropdown) has the same density. Check once on
      Windows at 100%.
  Seen (lead's smoke, 2026-09-25): items in text-xs, the Rating heading dimmer and smaller;
  pills `G S Q E none`, the current one filled; hovering Q kept its orange under a ring;
  clicking q rated the tile (sidebar Q count 4 → 5) and closed the menu; "Move to trash" red
  with the trash icon; a checked collection in the submenu kept its right gutter under a long
  name; the Library dropdown (bottom-left) has the same density. Not seen: Windows at 100%.
  Screenshots: `boorubox-vault/smoke-2026-09-25/A1-tilemenu-zoom.png`, `A2-pill-hover.png`, `A4-sub-checked.png`,
  `A6-q-filled.png`, `A7-library-dropdown.png`.

  Hand check: not run against the live app (no dev server, per the gate boundary). Look at,
  on a right-click tile menu: the "Rating" heading is dim/small against the pills below it; the
  five pills (`g` `s` `q` `e` `none`) sit in one row, the image's current rating filled solid in
  its colour and the rest outlined in the same dim colour, `none` filled in `bg-secondary` when
  the image is unrated; typing `q` then Enter, or clicking `q`, rates and closes the menu (a
  letter alone only highlights the pill in bits-ui); arrow keys still step through
  every item including the pill row (bits-ui roving focus in DOM order, not verified visually);
  "Move to trash" is red with a trash icon and fires on one click, no confirmation. Also look at:
  whether the four filled rating pills need a `border-transparent` (like `RatingPills.svelte`'s
  own active state adds) — `RATING_COLOUR`'s `!`-forced background may still show the default
  `border-border` edge around it, which D5 does not call out explicitly (see Handoff below).
  Check the same density on the Library menu (top-left dropdown) and once on Windows at 100%.

## Handoff

(units write here)

### Unit A

- `#n` hint class (span): `absolute top-0 right-0 text-[10px] text-muted-foreground
  tabular-nums`, plus `mt-1.5` on the span itself for a row past the first (matching the `<ul>`'s
  own `pt-1.5`, so the hint aligns with the first line of chips rather than sitting at the
  hairline). The `<ul>` carries `relative` always and `pr-6` (the reserved 1.5rem from the
  design's risk note) only when `vocabulary.pinnedGroups.length > 1`; `aria-label` on the `<ul>`
  and `aria-hidden="true"` on the span follow the same condition.
- No deviations from D1/D2/D4/D7. `TagSidebar.svelte` needed no change: it only mounts
  `TagVocabularyMenuItems`, so the Danbooru item and category icons appear there for free.
- Gate: `pnpm lint` clean, `pnpm typecheck` 0 errors, `pnpm test` — app 58 files / 716 tests
  passed (includes the new `danbooru.test.ts`, 4 tests).

### Unit B

- The pills needed the `!` override, but only where the pill has no forced background: the four
  ratings' active state (`RATING_COLOUR[r]`, already `bg-*-500!`) already outranks
  `focus:bg-accent` on its own, so only the *dim* rating state and the "none" pill's *rated*
  (non-filled) state carry `focus:bg-transparent!`. The unrated "none" state
  (`bg-secondary text-secondary-foreground`) has a real background too and needed no override.
- Exact pill classes (all five items share this base, `cn`/`twMerge` resolves the conflicts
  against the item's own defaults):
  `justify-center rounded-md border px-1.5 py-0.5 text-xs font-semibold uppercase focus:ring-1
  focus:ring-ring`, plus per state: active rating → `RATING_COLOUR[rating]`; dim rating →
  `` `${RATING_COLOUR_DIM[rating]} focus:bg-transparent!` ``; unrated "none" →
  `bg-secondary text-secondary-foreground`; rated "none" →
  `border-border text-muted-foreground focus:bg-transparent!`.
- D5 lists only `RATING_COLOUR[r]` / `RATING_COLOUR_DIM[r]` for the four active/dim pills, with
  no `border-transparent` — but `ImageCard.svelte`'s class strings above already carry it, on
  the active-rating branch and the unrated "none" branch, the same fix `RatingPills.svelte`'s
  own active state uses: a filled pill's base `border` utility would otherwise resolve to the
  app-wide `border-border` (set on `*` in `app.css`'s base layer) and show as a stray ring around
  the pill's fill. Left as a hand-check line under 2.7 for the light-border question anyway,
  since the two pills that are not `border-transparent` (the dim ratings, the rated "none") were
  never meant to be.
- `app.css`'s `[data-slot='context-menu-label']` and `[data-slot='dropdown-menu-label']`
  selectors also dim those slots, which already shipped their own `text-xs text-muted-foreground`
  classes in the copy-in — the new rule is redundant there but harmless (same values), so no
  exception was carved out for them.
- No deviations on 2.3/2.4/2.5. `actions.trash`'s own confirmation gate (`trash` D12, amended,
  two-or-more images) is untouched — this unit only touched colour, icon and copy on the menu
  item, never the write path.
- Gate: `pnpm lint` clean; `pnpm typecheck` — 1196 files, 0 errors; `pnpm -r test` — shared 1/1,
  extension 94/94, app 716/716, all passed.
