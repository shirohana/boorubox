> One Sonnet unit (retried on Opus if the gate fails), then an Opus review shared with
> `menu-polish`. Design D1–D2 decide the shape; do not re-decide them. This unit owns
> `packages/app/src/lib/components/rules/RulesTable.svelte` (renamed), `RulesSection.svelte`
> and the `auto-tag-rules` archive design. `menu-polish` units are editing `Inspector.svelte`,
> `ImageCard.svelte`, `app.css`, `TagVocabularyMenuItems.svelte`, `CollectionsSection.svelte`,
> `StampBar.svelte` at the same time: do not touch those. Gate: `pnpm lint && pnpm typecheck &&
> pnpm test` from the repo root; `.svelte-kit` is regenerated by typecheck and test, so run the
> gate once at the end and, if it fails on a generated-file error while another unit is also
> gating, rerun it once. No commit, no ticked hand check.

## 1. Unit C — the rules list (`packages/app`)

- [x] 1.1 `git mv RulesTable.svelte RuleList.svelte`; markup per D1, script untouched except
      the header comment (say what it is now: a stacked list, why not a table — 672px column,
      five columns never fit). `RulesSection.svelte` imports and mounts `RuleList`.
- [x] 1.2 `openspec/changes/archive/2026-09-1*-auto-tag-rules/design.md` D11 (find the folder
      under `openspec/changes/archive/` whose name ends in `auto-tag-rules`): append a dated
      paragraph — "the legacy's table" became a stacked list on 2026-09-25 because the settings
      column is 672px and the table scrolled sideways.
- [x] 1.3 Gate green. Handoff: the final class strings of the entry and the `dl`.
- [ ] 1.4 Hand check (owner): Settings → Rules with a rule of ten tags and a long regex shows
      every control without a sideways scroll; New and regex badges, the invalid line, the
      switch, edit and delete all still work.
  Seen (lead's smoke, 2026-09-25): a rule with a ~100-character regex and ten tags rendered as
  one bordered entry — name and regex badge, switch, edit and delete on the header line, the
  pattern wrapping and the chips on two lines — with no sideways scroll at 1200 px and at the
  900 px minimum window width. Screenshots: `boorubox-vault/smoke-2026-09-25/E3-rule-list.png`, `E4-rule-narrow.png`.
  Noticed, not changed: the empty state repeats the section's own paragraph.

Hand check: not run against the live app (no dev server per the unit's boundaries) — owner to
confirm a rule with a long regex pattern and ten tags at the settings page's width shows the
name, both badges, the invalid line, the switch and the edit/delete buttons with no sideways
scroll, and that toggling, editing and deleting still work.

## Handoff

- Entry: `<li class="rounded-lg border border-border p-3">` inside `<ul class="flex flex-col
  gap-2">`. Header row: `<div class="flex flex-wrap items-center gap-2">` with the name as
  `<span class="text-sm font-medium">`, the New and regex `Badge`s inline, then `<div
  class="ml-auto flex items-center gap-1">` holding the `Switch` and the two `Button`s
  (`size="icon-xs" variant="ghost"`).
- `dl`: `<dl class="mt-2 grid grid-cols-[auto_minmax(0,1fr)] gap-x-3 gap-y-1 text-xs">`, `dt`
  as `text-muted-foreground`, `dd` as `min-w-0`.
- Deviation: D1 groups the regex badge with the header's badges rather than leaving it beside
  the pattern (the table had it there); doing so lets the header's `ml-auto` push the switch
  and buttons to the line's end even when the pattern wraps below, matching D1's own wording
  ("the regex badge, then `ml-auto` the `Switch`...").
- Open item (not touched, outside this unit's ownership): two comments elsewhere still name
  `RulesTable` by its old filename — `packages/app/src/lib/components/stamps/StampsTable.svelte:95`
  and `packages/app/src/lib/api/collections.svelte.ts:14`. Both are outside this unit's file
  ownership (one is the read-only sibling, the other is unowned); worth a follow-up rename.
- `icon-xs` exists on `Button` (`packages/app/src/lib/components/ui/button/button.svelte`), so
  no substitution was needed.
