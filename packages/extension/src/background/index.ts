// Service worker: context menu, capture fallback (fetch + Referer rule),
// delivery to the app, Download History. Capture code is ported from the
// legacy extension's src/background/index.ts, not rewritten.
export {}
